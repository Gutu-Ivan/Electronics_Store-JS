const ROOT_PRODUCTS = document.getElementById('products');
const ROOT_SPINNER = document.getElementById('spinner');
const ROOT_CART = document.getElementById('main_cart');
const ROOT_CART_ITEMS = document.getElementById('cart_items');
const ROOT_CART_COUNT = document.querySelector('.cart_count');
const ROOT_TOTAL = document.getElementById('cart_total')
const ROOT_ORDER_PRODUCTS = document.getElementById('order_products');
const ROOT_ORDER_TOTAL = document.getElementById('order_total');